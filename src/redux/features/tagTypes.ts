export enum tagTypes {
  user = "user",
  admin = "admin",
  super_admin = "super_admin",
  service = "service",
}

export const tagTypeList = [
  tagTypes.user,
  tagTypes.admin,
  tagTypes.super_admin,
  tagTypes.service,
];
